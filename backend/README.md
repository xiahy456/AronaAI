# 后端说明
## 目录结构
├── `backend`
    ├── `app` --后端组件
    ├── `tests` --测试
    ├── `data` --数据
    ├── `utils` --工具
    ├── `third-party` --第三方
    ├── `README.md` --后端说明文档
    └── `main.py` --程序入口

## 后端分层架构
    1. presentation 前端交互层
    2. memory 记忆搜索层
    3. emotion 情感判定层
    4. interact 模型交互层
